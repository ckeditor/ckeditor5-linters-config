---
type: Feature
scope:
  - eslint-plugin-ckeditor5-rules
---

Extended the `ckeditor5-rules/license-header` rule to also cover CSS files (via `@eslint/css`). The same rule now enforces the configured license header on JavaScript, TypeScript, and CSS sources.
