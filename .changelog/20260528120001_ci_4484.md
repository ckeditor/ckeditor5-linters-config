---
type: Feature
scope:
  - eslint-plugin-ckeditor5-rules
---

Extended `ckeditor5-rules/license-header` to validate CSS files. The CSS branch identifies the license header as the first block comment containing an `@license` tag, mirroring the JavaScript detection. Missing, incorrect, or wrongly-positioned headers are reported and auto-fixed.
