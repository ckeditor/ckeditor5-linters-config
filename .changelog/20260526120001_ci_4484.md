---
type: Feature
scope:
  - eslint-plugin-ckeditor5-rules
---

Added a new `ckeditor5-rules/no-disallowed-color-formats` rule for CSS files. The rule rejects hex colors, `rgb()` and `rgba()` calls, and named CSS colors. Use HSL or CSS custom properties instead.
