---
type: Feature
scope:
  - eslint-plugin-ckeditor5-rules
---

Added a new `ckeditor5-rules/ck-content-variable-name` rule for CSS files. The rule enforces that CSS custom properties consumed inside the `.ck-content` selector use the `--ck-content-*` prefix.
