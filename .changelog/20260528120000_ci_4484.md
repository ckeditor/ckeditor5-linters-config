---
type: Feature
scope:
  - eslint-plugin-ckeditor5-rules
---

Added a new `ckeditor5-rules/ck-content-variable-name` rule for CSS files. The rule requires variables used inside `.ck-content` selectors to use the `--ck-content-*` prefix. Exemptions can be configured with the `ignoredVariableSubstrings` option.
